name: Build Windows EXE

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Create .env file
        run: |
          echo "VITE_SUPABASE_URL=${{ secrets.VITE_SUPABASE_URL }}" > .env
          echo "VITE_SUPABASE_PUBLISHABLE_KEY=${{ secrets.VITE_SUPABASE_ANON_KEY }}" >> .env
          echo "VITE_SUPABASE_PROJECT_ID=${{ secrets.VITE_SUPABASE_PROJECT_ID }}" >> .env

      - name: Build Vite (React)
        run: npm run build:electron

      - name: Obfuscate JS bundles
        run: node electron/scripts/obfuscate.cjs

      - name: Generate integrity manifest
        run: node electron/scripts/generate-integrity.cjs

      - name: Build Electron Windows EXE
        run: npx electron-builder --win --config electron-builder.yml --publish never

      - name: Upload Windows release files
        uses: actions/upload-artifact@v4
        with:
          name: School-Cashbox-Windows-Release
          path: release/*
          retention-days: 30
